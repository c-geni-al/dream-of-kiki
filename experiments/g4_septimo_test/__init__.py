"""G4-septimo pilot — H6-B Tiny-ImageNet RECOMBINE-strategy placebo.

Resolves the H6-C universality conjunction deferred by the G4-sexto
aggregator (commit ``0728a18``) by adding the H6-B clause on
Split-Tiny-ImageNet 200-class / 64×64 RGB scale with the
``G4MediumCNN`` substrate (3 Conv2d + 3 MaxPool2d + 2 Linear,
``latent_dim = 128``, ``n_classes = 20`` per task).

Pre-registration : ``docs/osf-prereg-g4-septimo-pilot.md``.
"""
